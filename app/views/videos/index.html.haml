%article.video_category
  %header
    %h3= link_to "Category"
  .videos.row
    - @videos.each do |video|
      .video.col-sm-2
        = link_to image_tag("/tmp/#{video.title.downcase.gsub(' ','_')}.jpg"), video_path(video)